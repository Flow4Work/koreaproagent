const GROQ_URL='https://api.groq.com/openai/v1/chat/completions';
const GROQ_VERSION='2025-08-16';
function clean(v,max=1200){return typeof v==='string'?v.trim().slice(0,max):''}
function parseJson(text){try{return JSON.parse(text)}catch{}const s=String(text||'');const f=s.match(/```(?:json)?\s*([\s\S]*?)```/i);if(f){try{return JSON.parse(f[1])}catch{}}const a=s.indexOf('{'),b=s.lastIndexOf('}');if(a>=0&&b>a)return JSON.parse(s.slice(a,b+1));throw new Error('Groq returned invalid JSON')}
function rateInfo(r){return{remaining_requests:r.headers.get('x-ratelimit-remaining-requests'),remaining_tokens:r.headers.get('x-ratelimit-remaining-tokens'),retry_after:r.headers.get('retry-after')}}
async function callGroq({model,prompt,timeoutMs}){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),timeoutMs),started=Date.now();try{const response=await fetch(GROQ_URL,{method:'POST',headers:{Authorization:`Bearer ${process.env.GROQ_API_KEY}`,'Content-Type':'application/json','Groq-Model-Version':GROQ_VERSION},body:JSON.stringify({model,messages:[{role:'system',content:'현재 공개 웹 근거만 사용하는 B2B 리서처다. 사실과 URL을 절대 지어내지 말고 JSON 하나만 반환한다.'},{role:'user',content:prompt}],temperature:.05,response_format:{type:'json_object'},citation_options:'disabled',compound_custom:{tools:{enabled_tools:['web_search']}}}),signal:controller.signal});const raw=await response.text();if(!response.ok){let detail=raw;try{detail=JSON.parse(raw)?.error?.message||raw}catch{}const e=new Error(`Groq HTTP ${response.status}: ${clean(detail,500)}`);e.status=response.status;e.retryAfter=response.headers.get('retry-after');e.rate=rateInfo(response);throw e}const payload=JSON.parse(raw);return{data:parseJson(payload?.choices?.[0]?.message?.content),durationMs:Date.now()-started,toolCalls:Array.isArray(payload?.choices?.[0]?.message?.executed_tools)?payload.choices[0].message.executed_tools.length:0,rate:rateInfo(response),usage:payload?.usage||null}}catch(error){if(error?.name==='AbortError'){const e=new Error(`Groq timeout after ${Math.round(timeoutMs/1000)}s`);e.status=504;throw e}throw error}finally{clearTimeout(timer)}}
export async function POST(request){if(!process.env.GROQ_API_KEY)return Response.json({error:'GROQ_API_KEY is missing.'},{status:503});let body={};try{body=await request.json()}catch{return Response.json({error:'Invalid request body.'},{status:400})}const count=Math.max(3,Math.min(5,Number.parseInt(body.count,10)||3)),focus=clean(body.focus,1800),mode=body.mode==='deep'?'deep':'fast';const prompt=`목표: 우리가 Korea Pipeline Pilot을 제안할 해외 B2B SaaS/AI 회사 ${count}곳을 찾는다.\n\n우리가 파는 것: 한국팀을 채용하기 전에 한국에서 실제로 팔릴 가능성을 시험하도록 한국 잠재고객, 접촉 이유, 담당 역할, 영업문을 만들어주는 390,000원 파일럿.\n\n찾을 조건: ${focus||'Seed~Series B B2B SaaS/AI. 최근 투자, APAC/일본/싱가포르 진출, 해외 세일즈·파트너십 채용 등 현재 확장 신호가 있고 한국 영업조직은 아직 크지 않으며 한국 B2B 사용처가 명확한 회사.'}\n\n규칙:\n1. 현재 웹검색으로 확인한다. 최근 신호는 가능하면 최근 18개월 이내만 쓴다.\n2. 대기업이나 이미 한국 지사가 성숙한 회사는 제외한다.\n3. 후보마다 실제 근거 URL이 최소 1개 없으면 후보에 넣지 않는다.\n4. 자금조달·채용·진출·출시를 확인하지 못했으면 추측하지 않는다.\n5. 설명 필드는 전부 한국어로 쓴다. 회사명/URL/검색쿼리만 원문 허용.\n6. 한국 고객 샘플은 아직 찾지 않는다.\n\nJSON만 반환:\n{"candidates":[{"company":"","url":"","country":"한국어 국가명","category":"한국어 업종","trigger":"한국어 한두 문장","source_urls":[],"recommended_role":"한국어 직책","contact_search_query":""}],"strategy":{"best_segment":"한국어 한 문장","pitch":"한국어 한 문장"}}`;
const attempts=mode==='deep'?[{model:'groq/compound',timeoutMs:22000},{model:'groq/compound-mini',timeoutMs:14000}]:[{model:'groq/compound-mini',timeoutMs:14000},{model:'groq/compound',timeoutMs:18000}],failures=[];for(let i=0;i<attempts.length;i++){const attempt=attempts[i];try{const result=await callGroq({...attempt,prompt}),data=result.data;const candidates=Array.isArray(data?.candidates)?data.candidates.slice(0,count).map(c=>({company:clean(c?.company,140),url:clean(c?.url,350),country:clean(c?.country,80),category:clean(c?.category,120),trigger:clean(c?.trigger,700),source_urls:Array.isArray(c?.source_urls)?c.source_urls.map(String).filter(x=>/^https?:\/\//i.test(x)).slice(0,5):[],recommended_role:clean(c?.recommended_role,120),contact_search_query:clean(c?.contact_search_query,280)})).filter(c=>c.company&&/^https?:\/\//i.test(c.url)&&c.source_urls.length):[];if(!candidates.length)throw new Error('No candidates with usable evidence');return Response.json({candidates,strategy:data?.strategy||{},meta:{model:attempt.model,mode,fallback_used:i>0,duration_ms:result.durationMs,tool_calls:result.toolCalls,rate:result.rate,usage:result.usage}},{headers:{'Cache-Control':'no-store'}})}catch(error){failures.push(clean(error?.message||'unknown error',600));if([401,403].includes(error?.status))return Response.json({error:failures.at(-1),hint:'Groq API key permission/authentication failed.'},{status:502});if(error?.status===429)return Response.json({error:failures.at(-1),hint:`Groq rate limit reached. Wait ${error.retryAfter||'a few'} seconds and retry.`,rate:error.rate||null},{status:429,headers:{'Cache-Control':'no-store'}})}}return Response.json({error:`Candidate discovery failed. ${failures.join(' | ')}`,hint:'진단이 정상이면 빠르게 + 3곳으로 다시 실행하세요.'},{status:502,headers:{'Cache-Control':'no-store'}})}
