const ZEN_CHAT_URL='https://opencode.ai/zen/v1/chat/completions';
const ZEN_MODELS_URL='https://opencode.ai/zen/v1/models';

export const AI_PROVIDER='opencode-zen';
export const AI_MODEL=process.env.OPENCODE_ZEN_MODEL||'deepseek-v4-flash-free';
export const AI_MODELS=[...new Set([AI_MODEL,'deepseek-v4-flash-free','mimo-v2.5-free','ling-3.0-flash-free'].filter(Boolean))];

export function aiConfigured(){return Boolean(process.env.OPENCODE_ZEN_API_KEY)}
function redact(value=''){return String(value).replace(/Bearer\s+[A-Za-z0-9._-]+/gi,'Bearer [redacted]').replace(/[A-Za-z0-9_-]{32,}/g,'[key]').slice(0,700)}
function extractJson(text=''){const raw=String(text||'').trim();if(!raw)throw new Error('AI 응답이 비어 있습니다.');try{return JSON.parse(raw)}catch{}const fenced=raw.match(/```(?:json)?\s*([\s\S]*?)```/i)?.[1]?.trim();if(fenced){try{return JSON.parse(fenced)}catch{}}const start=raw.indexOf('{'),end=raw.lastIndexOf('}');if(start>=0&&end>start){try{return JSON.parse(raw.slice(start,end+1))}catch{}}throw new Error('AI가 유효한 JSON을 반환하지 않았습니다.')}
function contentText(content){if(typeof content==='string')return content.trim();if(!Array.isArray(content))return'';return content.map(part=>{if(typeof part==='string')return part;if(part?.type==='text'&&typeof part?.text==='string')return part.text;return''}).filter(Boolean).join('\n').trim()}
function reasoningText(message={}){for(const value of[message.reasoning_content,message.reasoning,message.thinking])if(typeof value==='string'&&value.trim())return value.trim();return''}

async function callZen({model,prompt,maxTokens,timeoutMs,temperature}){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),timeoutMs);try{const response=await fetch(ZEN_CHAT_URL,{method:'POST',headers:{Authorization:`Bearer ${process.env.OPENCODE_ZEN_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({model,messages:[{role:'system',content:'Return only valid JSON. Do not wrap JSON in markdown.'},{role:'user',content:prompt}],temperature,max_tokens:maxTokens}),signal:controller.signal,cache:'no-store'});const raw=await response.text();if(!response.ok){let detail=raw;try{detail=JSON.parse(raw)?.error?.message||raw}catch{}const e=new Error(`OpenCode Zen HTTP ${response.status}: ${redact(detail)}`);e.status=response.status;e.retryAfter=response.headers.get('retry-after');throw e}const payload=JSON.parse(raw),choice=payload?.choices?.[0]||{},message=choice?.message||{};return{payload,content:contentText(message.content),reasoning:reasoningText(message),finishReason:choice?.finish_reason||'',model:payload?.model||model}}catch(e){if(e?.name==='AbortError'){const x=new Error('OpenCode Zen 응답 시간이 초과되었습니다.');x.status=504;throw x}throw e}finally{clearTimeout(timer)}}

export async function chatJson({prompt,maxTokens=1800,timeoutMs=35000,temperature=0}){if(!aiConfigured()){const e=new Error('OPENCODE_ZEN_API_KEY가 Vercel 환경변수에 없습니다.');e.status=503;throw e}const effectiveTimeout=Math.max(50000,timeoutMs);let lastError=null;for(let modelIndex=0;modelIndex<AI_MODELS.length;modelIndex++){const model=AI_MODELS[modelIndex];const budgets=modelIndex===0?[Math.max(8192,maxTokens),Math.max(16384,maxTokens*2)]:[Math.max(8192,maxTokens)];for(let attempt=0;attempt<budgets.length;attempt++){let result;try{result=await callZen({model,prompt,maxTokens:budgets[attempt],timeoutMs:effectiveTimeout,temperature})}catch(e){lastError=e;if([401,403].includes(Number(e?.status)))throw e;break}for(const text of[result.content,result.reasoning].filter(Boolean)){try{return{data:extractJson(text),usage:result.payload?.usage||null,model:result.model||model,finishReason:result.finishReason,usedReasoningFallback:!result.content&&Boolean(result.reasoning),fallbackModel:modelIndex>0}}catch(e){lastError=e}}const shouldRetry=attempt===0&&budgets.length>1&&(!result.content||result.finishReason==='length');if(!shouldRetry)break}}const e=new Error(lastError?.message||'사용 가능한 AI 모델이 유효한 JSON 답변을 완성하지 못했습니다. 다시 실행해주세요.');e.status=Number(lastError?.status)||502;throw e}

export async function checkAiConnection(timeoutMs=7000){if(!aiConfigured())return{ok:false,configured:false,available:false,error:'OPENCODE_ZEN_API_KEY is missing'};const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),timeoutMs);try{const response=await fetch(ZEN_MODELS_URL,{headers:{Authorization:`Bearer ${process.env.OPENCODE_ZEN_API_KEY}`},signal:controller.signal,cache:'no-store'});const raw=await response.text();if(!response.ok){let detail=raw;try{detail=JSON.parse(raw)?.error?.message||raw}catch{}return{ok:false,configured:true,available:false,status:response.status,error:redact(detail)}}const payload=JSON.parse(raw),ids=new Set(Array.isArray(payload?.data)?payload.data.map(x=>x?.id).filter(Boolean):[]),availableModels=AI_MODELS.filter(x=>ids.has(x));return{ok:availableModels.length>0,configured:true,available:availableModels.length>0,modelCount:ids.size,availableModels,preferredModel:availableModels[0]||AI_MODEL}}catch(e){return{ok:false,configured:true,available:false,error:e?.name==='AbortError'?'OpenCode Zen connection check timed out':redact(e?.message||e)}}finally{clearTimeout(timer)}}