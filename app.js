const $ = (id) => document.getElementById(id);
const APP_VERSION = '20260727-8';
const state = { workflow: 'sales', salesData: null, salesTab: 'leads', deliveryData: null, deliveryTab: 'prospects' };

if (localStorage.getItem('kpa.app.version') !== APP_VERSION) {
  ['kpa.sales.result','kpa.sales.form','kpa.delivery.result'].forEach((key) => localStorage.removeItem(key));
  localStorage.setItem('kpa.app.version', APP_VERSION);
}

function escapeHtml(v=''){return String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));}
function safeUrl(v=''){try{const u=new URL(v);return ['http:','https:'].includes(u.protocol)?u.href:''}catch{return''}}
function host(v=''){try{return new URL(v).hostname.replace(/^www\./,'')}catch{return v}}
function metric(label,value){return `<div class="metric"><span>${escapeHtml(label)}</span><strong>${escapeHtml(value||'-')}</strong></div>`}
function sourceLinks(urls=[]){return `<div class="source-links">${urls.map((u,i)=>{const s=safeUrl(u);return s?`<a href="${escapeHtml(s)}" target="_blank" rel="noopener noreferrer">근거 ${i+1}</a>`:''}).join('')}</div>`}
function csvCell(v){const s=Array.isArray(v)?v.join(' | '):String(v??'');return `"${s.replace(/"/g,'""')}"`}
function download(name,type,content){const blob=new Blob([content],{type});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}
function googleSearchUrl(q=''){return `https://www.google.com/search?q=${encodeURIComponent(q)}`}
async function copyText(text,btn){await navigator.clipboard.writeText(text||'');const old=btn.textContent;btn.textContent='복사됨';setTimeout(()=>btn.textContent=old,900)}

async function readJsonResponse(response){const text=await response.text();let data={};try{data=text?JSON.parse(text):{}}catch{const e=new Error(`API 응답 형식 오류 (HTTP ${response.status})`);e.status=response.status;throw e}if(!response.ok){const e=new Error(`${data?.error||data?.message||`HTTP ${response.status}`}${data?.hint?` · ${data.hint}`:''}`);e.status=response.status;e.data=data;throw e}return data}
async function requestJson(url,payload,timeoutMs){const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),timeoutMs);try{return await readJsonResponse(await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload),cache:'no-store',signal:controller.signal}))}catch(error){if(error?.name==='AbortError'){const e=new Error(`조사가 ${Math.round(timeoutMs/1000)}초를 넘어 중단됐습니다.`);e.status=504;throw e}throw error}finally{clearTimeout(timer)}}

async function checkHealth(){try{const d=await readJsonResponse(await fetch(`/api/health?t=${Date.now()}`,{cache:'no-store'}));const ok=Boolean(d.groqConnected);const label=ok?(d.tavilyConfigured?'Tavily + Groq 정상':'Groq 정상'):'API 오류';$('apiStatus').className=`status ${ok?'ok':'bad'}`;$('apiStatus').title=ok?`검색: ${d.searchProvider||'Groq'} · 정리: Groq`:(d.error||'API 연결 실패');$('apiStatus').innerHTML=`<span class="dot"></span><span>${label}</span>`;return ok}catch(error){$('apiStatus').className='status bad';$('apiStatus').title=error.message;$('apiStatus').innerHTML='<span class="dot"></span><span>API 오류</span>';return false}}
function diagLine(name,check){const ok=check?.ok===true,skipped=check?.skipped===true;const mark=skipped?'–':ok?'✓':'✕';const cls=skipped?'':ok?'diag-ok':'diag-bad';const detail=[check?.status?`HTTP ${check.status}`:'',check?.duration_ms?`${check.duration_ms}ms`:'',check?.rate?.remaining_tokens?`잔여 토큰 ${check.rate.remaining_tokens}`:''].filter(Boolean).join(' · ');return `<div class="diag-row"><b class="${cls}">${mark}</b><span>${escapeHtml(name)}</span><small>${escapeHtml(detail||check?.message||'')}</small></div>`}
async function runDiagnostics(){const btn=$('diagBtn'),panel=$('diagPanel');btn.disabled=true;btn.textContent='확인 중';panel.classList.remove('hidden');panel.innerHTML='<strong>API 키와 모델 연결을 확인합니다.</strong>';try{const [diagRes,healthRes]=await Promise.all([fetch(`/api/diagnostics?t=${Date.now()}`,{cache:'no-store'}),fetch(`/api/health?t=${Date.now()}`,{cache:'no-store'})]);const d=JSON.parse(await diagRes.text());const h=JSON.parse(await healthRes.text());const c=d.checks||{};panel.innerHTML=`<div class="diag-head"><strong>${d.ok&&h.groqConnected?'정상':'문제 발견'}</strong><button id="diagClose" class="ghost small">닫기</button></div><div class="diag-row"><b class="${h.tavilyConfigured?'diag-ok':'diag-bad'}">${h.tavilyConfigured?'✓':'✕'}</b><span>Tavily 검색</span><small>${h.tavilyConfigured?'키 설정됨':'TAVILY_API_KEY 확인 필요'}</small></div>${diagLine('Groq 인증',c.auth)}`;$('diagClose')?.addEventListener('click',()=>panel.classList.add('hidden'))}catch(e){panel.innerHTML=`<div class="diag-head"><strong>진단 실패</strong><button id="diagClose" class="ghost small">닫기</button></div><p>${escapeHtml(e.message)}</p>`;$('diagClose')?.addEventListener('click',()=>panel.classList.add('hidden'))}finally{btn.disabled=false;btn.textContent='진단'}}

function setWorkflow(name){state.workflow=name;$('salesWorkspace').classList.toggle('hidden',name!=='sales');$('deliveryWorkspace').classList.toggle('hidden',name!=='delivery');document.querySelectorAll('.workflow-btn').forEach(b=>b.classList.toggle('active',b.dataset.workflow===name));localStorage.setItem('kpa.workflow',name)}
document.querySelectorAll('.workflow-btn').forEach(b=>b.addEventListener('click',()=>setWorkflow(b.dataset.workflow)));

function setSalesBusy(on){$('salesRunBtn').disabled=on;$('salesRunBtn').querySelector('span').textContent=on?'검색 + 재검증 중…':'검증된 해외 고객 찾기';if(on){$('salesEmpty').classList.add('hidden');$('salesError').classList.add('hidden');$('salesResults').classList.add('hidden');$('salesLoading').classList.remove('hidden')}else{$('salesLoading').classList.add('hidden')}}
function salesError(msg){$('salesLoading').classList.add('hidden');$('salesResults').classList.add('hidden');$('salesEmpty').classList.add('hidden');$('salesError').textContent=msg;$('salesError').classList.remove('hidden')}
async function runSales(){const focus=$('salesFocus').value.trim();localStorage.setItem('kpa.sales.form',JSON.stringify({focus}));state.salesData=null;setSalesBusy(true);try{if(!await checkHealth())throw new Error('API 연결 오류입니다. 우측 상단 진단을 확인하세요.');const d=await requestJson('/api/discover-clients',{focus},105000);if(!Array.isArray(d?.leads)||!d.leads.length)throw new Error('검증 가능한 해외 고객을 찾지 못했습니다.');state.salesData=d;state.salesTab='leads';localStorage.setItem('kpa.sales.result',JSON.stringify(d));renderSales()}catch(e){salesError(e.message||'고객 발굴에 실패했습니다.')}finally{setSalesBusy(false);if(state.salesData?.leads?.length)$('salesResults').classList.remove('hidden')}}

function renderSales(){const d=state.salesData;if(!d)return;$('salesEmpty').classList.add('hidden');$('salesError').classList.add('hidden');$('salesLoading').classList.add('hidden');$('salesResults').classList.remove('hidden');$('salesCsv').disabled=false;const top=d.leads?.[0];const provider=d.meta?.search?.provider==='tavily'?'Tavily → Groq → 재검증':'웹검색 → Groq → 재검증';$('salesSummary').innerHTML=metric('검증 통과',`${d.leads.length}곳`)+metric('1위 점수',top?`${top.fit_score}/100`:'-')+metric('방식',provider);$('salesNext').innerHTML=top?`<div><strong>다음: ${escapeHtml(top.company)} 담당자를 확인하고 첫 메일 발송</strong><p>회사 홈페이지와 근거를 확인한 뒤 ‘담당자 찾기’ → ‘영업문 복사’ 순서로 진행하세요.</p></div><button class="primary small" id="topMailBtn">영업문 보기</button>`:'';$('topMailBtn')?.addEventListener('click',()=>{state.salesTab='messages';renderSales()});document.querySelectorAll('[data-sales-tab]').forEach(b=>b.classList.toggle('active',b.dataset.salesTab===state.salesTab));renderSalesTab()}
function renderSalesTab(){const d=state.salesData;if(!d)return;const root=$('salesContent');if(state.salesTab==='leads'){root.innerHTML=`<div class="buyer-list">${d.leads.map(l=>{const q=l.contact_search_query||`${l.company} ${l.recommended_role||'Head of Sales'} LinkedIn`;return `<article class="buyer-card"><div class="buyer-head"><div><span class="rank">#${l.rank} · ${escapeHtml(l.country||'')}</span><h3>${escapeHtml(l.company)}</h3>${safeUrl(l.url)?`<a href="${escapeHtml(safeUrl(l.url))}" target="_blank" rel="noopener noreferrer">${escapeHtml(host(l.url))}</a>`:''}</div><span class="score big-score">${l.fit_score}</span></div><p class="buyer-summary">${escapeHtml(l.why_buy_our_service)}</p><div class="buyer-grid"><div><b>왜 지금 연락?</b><p>${escapeHtml(l.why_now)}</p>${sourceLinks(l.source_urls)}</div><div><b>누구에게?</b><p>${escapeHtml(l.recommended_role||'영업/사업개발 책임자')}</p></div></div><div class="korea-sample"><b>한국에서 팔릴 포인트</b><p>${escapeHtml(l.korea_opportunity)}</p></div><div class="buyer-actions"><button class="main-action lead-mail" data-rank="${l.rank}">영업문 보기</button><button class="lead-copy" data-rank="${l.rank}">영업문 복사</button><a href="${escapeHtml(googleSearchUrl(q))}" target="_blank" rel="noopener noreferrer">담당자 찾기</a>${safeUrl(l.url)?`<a href="${escapeHtml(safeUrl(l.url))}" target="_blank" rel="noopener noreferrer">공식 홈페이지</a>`:''}</div>${l.warning?`<p class="warning-text">주의: ${escapeHtml(l.warning)}</p>`:''}</article>`}).join('')}</div>`;root.querySelectorAll('.lead-mail').forEach(btn=>btn.addEventListener('click',()=>{state.salesTab='messages';renderSales();setTimeout(()=>document.querySelector(`[data-message-rank="${btn.dataset.rank}"]`)?.scrollIntoView({behavior:'smooth',block:'center'}),30)}));root.querySelectorAll('.lead-copy').forEach(btn=>btn.addEventListener('click',()=>{const l=d.leads.find(x=>x.rank===Number(btn.dataset.rank));copyText(l?.outreach_en||'',btn)}))}else if(state.salesTab==='messages'){root.innerHTML=`<div class="cards">${d.leads.map(l=>`<article class="message-card" data-message-rank="${l.rank}"><div class="top"><div><span class="rank">#${l.rank}</span><h3>${escapeHtml(l.company)}</h3></div><button class="copy sales-copy" data-rank="${l.rank}">영문 복사</button></div><p class="subject">추천 대상: ${escapeHtml(l.recommended_role||'영업/사업개발 책임자')}</p><p class="message-text">${escapeHtml(l.outreach_en)}</p><details><summary>한국어 뜻</summary><p class="message-text">${escapeHtml(l.outreach_ko)}</p></details></article>`).join('')}</div>`;root.querySelectorAll('.sales-copy').forEach(btn=>btn.addEventListener('click',()=>{const l=d.leads.find(x=>x.rank===Number(btn.dataset.rank));copyText(l?.outreach_en||'',btn)}))}else{const top=d.leads?.[0],s=d.strategy||{};root.innerHTML=`<div class="action-steps"><div class="action-step"><b>1</b><div><strong>${escapeHtml(top?.company||'1위 회사')} 실제 담당자 확인</strong><span>Founder, Head of Sales, BD, Partnerships, Growth 중 맞는 사람을 찾습니다.</span></div></div><div class="action-step"><b>2</b><div><strong>무료 한국 기업 3곳 샘플 제안</strong><span>영업문을 복사해 보내고 답장 여부를 기록합니다.</span></div></div><div class="action-step"><b>3</b><div><strong>관심 답장 → 390,000원 Pilot 제안</strong><span>${escapeHtml(s.next_action||'답장이 오면 계약 후 납품 탭에서 한국 잠재고객 자료를 만듭니다.')}</span></div></div></div>`}}
function exportSalesCsv(){const d=state.salesData;if(!d)return;const cols=['rank','company','url','country','fit_score','why_buy_our_service','why_now','source_urls','recommended_role','contact_search_query','korea_opportunity','outreach_en','outreach_ko','confidence','warning'];const rows=[cols.join(','),...d.leads.map(l=>cols.map(c=>csvCell(l[c])).join(','))];download(`korea-sales-leads-${Date.now()}.csv`,'text/csv;charset=utf-8','\ufeff'+rows.join('\n'))}

const deliveryFields=['clientUrl','productHint','targetNotes','seeds'];
function saveDeliveryForm(){const v={};deliveryFields.forEach(id=>v[id]=$(id).value);localStorage.setItem('kpa.delivery.form',JSON.stringify(v))}
function setDeliveryBusy(on){$('runBtn').disabled=on;$('runBtn').querySelector('span').textContent=on?'검색 + 정리 중…':'한국 영업 후보 만들기';$('emptyState').classList.toggle('hidden',on||!!state.deliveryData);$('loadingState').classList.toggle('hidden',!on);$('errorState').classList.add('hidden');if(on)$('results').classList.add('hidden')}
function showDeliveryError(msg){$('loadingState').classList.add('hidden');$('emptyState').classList.add('hidden');$('results').classList.add('hidden');$('errorState').textContent=msg;$('errorState').classList.remove('hidden')}
async function runDelivery(){const clientUrl=$('clientUrl').value.trim();if(!clientUrl){showDeliveryError('고객 SaaS URL을 입력하세요.');return}saveDeliveryForm();setDeliveryBusy(true);try{if(!await checkHealth())throw new Error('API 연결 오류입니다.');const d=await requestJson('/api/analyze',{clientUrl,productHint:$('productHint').value,targetNotes:$('targetNotes').value,seeds:$('seeds').value,count:5},115000);state.deliveryData=d;state.deliveryTab='prospects';localStorage.setItem('kpa.delivery.result',JSON.stringify(d));renderDelivery()}catch(e){state.deliveryData=null;showDeliveryError(e.message||'분석에 실패했습니다.')}finally{setDeliveryBusy(false);if(state.deliveryData)$('results').classList.remove('hidden')}}
function renderDelivery(){const d=state.deliveryData;if(!d)return;$('emptyState').classList.add('hidden');$('loadingState').classList.add('hidden');$('errorState').classList.add('hidden');$('results').classList.remove('hidden');$('exportCsv').disabled=false;const top=d.prospects?.[0];$('summaryGrid').innerHTML=metric('고객',d.client?.name||host(d.client?.url))+metric('한국 후보',`${d.prospects?.length||0}곳`)+metric('1위 점수',top?`${top.fit_score}/100`:'-');document.querySelectorAll('[data-tab]').forEach(b=>b.classList.toggle('active',b.dataset.tab===state.deliveryTab));renderDeliveryTab()}
function renderDeliveryTab(){const d=state.deliveryData;if(!d)return;const root=$('tabContent');if(state.deliveryTab==='prospects'){root.innerHTML=`<div style="overflow:auto"><table class="prospect-table"><thead><tr><th>#</th><th>기업</th><th>점수</th><th>적합 이유</th><th>구매 신호</th><th>근거</th></tr></thead><tbody>${(d.prospects||[]).map(p=>`<tr><td>${p.rank}</td><td class="company-cell"><strong>${escapeHtml(p.company)}</strong>${safeUrl(p.url)?`<a target="_blank" rel="noopener noreferrer" href="${escapeHtml(safeUrl(p.url))}">${escapeHtml(host(p.url))}</a>`:''}</td><td><span class="score">${p.fit_score}</span></td><td>${escapeHtml(p.why_fit)}</td><td>${escapeHtml(p.buying_signal)}</td><td>${sourceLinks(p.source_urls)}</td></tr>`).join('')}</tbody></table></div>`}else if(state.deliveryTab==='contacts'){root.innerHTML=`<div class="cards">${(d.prospects||[]).map(p=>{const q=p.contact_search_query||`${p.company} ${p.recommended_role||'Head'} LinkedIn`;return `<article class="contact-card"><div class="top"><div><span class="rank">#${p.rank} · ${escapeHtml(p.company)}</span><h3>${escapeHtml(p.contact_name||p.recommended_role||'담당 직책 확인 필요')}</h3></div><a class="ghost small" href="${escapeHtml(googleSearchUrl(q))}" target="_blank" rel="noopener noreferrer">검색</a></div><p>${escapeHtml(q)}</p></article>`}).join('')}</div>`}else if(state.deliveryTab==='messages'){root.innerHTML=`<div class="cards">${(d.prospects||[]).map(p=>`<article class="message-card"><div class="top"><div><span class="rank">#${p.rank}</span><h3>${escapeHtml(p.company)}</h3></div><button class="copy delivery-copy" data-rank="${p.rank}">복사</button></div><p class="message-text">${escapeHtml(p.message_ko||p.message_en)}</p></article>`).join('')}</div>`;root.querySelectorAll('.delivery-copy').forEach(btn=>btn.addEventListener('click',()=>{const p=d.prospects.find(x=>x.rank===Number(btn.dataset.rank));copyText(p?.message_ko||p?.message_en||'',btn)}))}else{const s=d.strategy||{};root.innerHTML=`<div class="action-steps"><div class="action-step"><b>1</b><div><strong>상위 후보 근거 확인</strong><span>${escapeHtml(s.first_segment||'점수가 높은 기업부터 확인합니다.')}</span></div></div><div class="action-step"><b>2</b><div><strong>CSV 납품</strong><span>${escapeHtml(s.core_offer||'한국 후보와 메시지를 고객에게 전달합니다.')}</span></div></div><div class="action-step"><b>3</b><div><strong>운영 계약 제안</strong><span>${escapeHtml(s.next_action||'반응 데이터를 바탕으로 월 운영을 제안합니다.')}</span></div></div></div>`}}
function exportDeliveryCsv(){const d=state.deliveryData;if(!d)return;const cols=['rank','company','url','industry','fit_score','why_fit','buying_signal','source_urls','contact_name','contact_title','recommended_role','contact_search_query','sales_angle','message_ko','message_en'];const rows=[cols.join(','),...(d.prospects||[]).map(p=>cols.map(c=>csvCell(p[c])).join(','))];download(`korea-prospect-pack-${Date.now()}.csv`,'text/csv;charset=utf-8','\ufeff'+rows.join('\n'))}

$('diagBtn').addEventListener('click',runDiagnostics);
$('salesRunBtn').addEventListener('click',runSales);
$('salesCsv').addEventListener('click',exportSalesCsv);
$('salesDemo').addEventListener('click',()=>{$('salesFocus').value='Seed~Series B B2B SaaS/AI. 최근 투자 또는 APAC·일본·싱가포르 확장 신호가 있고, 한국 현지 법인·서울 오피스·한국 영업팀이 아직 없는 회사. 한국 B2B 고객에게 명확한 사용처가 있어야 함.'});
document.querySelectorAll('[data-sales-tab]').forEach(b=>b.addEventListener('click',()=>{state.salesTab=b.dataset.salesTab;renderSales()}));
$('runBtn').addEventListener('click',runDelivery);
$('exportCsv').addEventListener('click',exportDeliveryCsv);
$('fillDemo').addEventListener('click',()=>{$('clientUrl').value='https://www.intercom.com';$('productHint').value='AI 고객지원 자동화 SaaS';$('targetNotes').value='한국 이커머스 중 고객 문의량이 많거나 CS 채용이 늘어난 회사';saveDeliveryForm()});
document.querySelectorAll('[data-tab]').forEach(b=>b.addEventListener('click',()=>{state.deliveryTab=b.dataset.tab;renderDelivery()}));
deliveryFields.forEach(id=>$(id).addEventListener('change',saveDeliveryForm));
try{const f=JSON.parse(localStorage.getItem('kpa.sales.form')||'{}');if(f.focus!=null)$('salesFocus').value=f.focus}catch{}
try{const f=JSON.parse(localStorage.getItem('kpa.delivery.form')||'{}');deliveryFields.forEach(id=>{if(f[id]!=null)$(id).value=f[id]})}catch{}
try{const d=JSON.parse(localStorage.getItem('kpa.sales.result')||'null');if(d?.leads?.length){state.salesData=d;renderSales()}}catch{}
try{const d=JSON.parse(localStorage.getItem('kpa.delivery.result')||'null');if(d?.prospects?.length){state.deliveryData=d;renderDelivery()}}catch{}
setWorkflow(localStorage.getItem('kpa.workflow')==='delivery'?'delivery':'sales');
checkHealth();
